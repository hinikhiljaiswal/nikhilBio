{
  "name": "biometric-exam-verification",
  "version": "1.0.0",
  "private": true,
  "description": "Phase one web application for biometric student authentication and exam verification.",
  "workspaces": [
    "apps/api",
    "apps/web"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev -w apps/api\" \"npm run dev -w apps/web\"",
    "build": "npm run build -w apps/api && npm run build -w apps/web",
    "build:api": "npm run build -w apps/api",
    "build:web": "npm run build -w apps/web",
    "start:api": "npm run start -w apps/api",
    "start:web": "npm run start -w apps/web",
    "lint": "npm run lint -w apps/api && npm run lint -w apps/web",
    "test": "npm run test -w apps/api && npm run test -w apps/web",
    "qa": "npm run lint && npm run test && npm run build",
    "format": "prettier --write \"**/*.{ts,tsx,js,json,md,css}\""
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "prettier": "^3.6.2",
    "typescript": "^5.9.3"
  },
  "overrides": {
    "react": "19.2.7",
    "react-dom": "19.2.7",
    "postcss": "8.5.10"
  },
  "engines": {
    "node": ">=20.19.5 <25"
  },
  "dependencies": {
    "postcss": "8.5.10"
  }
}
